import { renderHook } from "@testing-library/react"

export default function Footer() {
    const the_style = {
        backgroundColor: "#e08178",
    }
    return (
        <footer style={the_style}>yeah</footer>
    )
}