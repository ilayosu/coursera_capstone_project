import { fireEvent, render, screen } from '@testing-library/react';
import Reservations from './Reservations';

test("initial times are correct", () => {
  // render(<Reservations/>);
  // const button_tester = screen.getByRole("button");
  // fireEvent.click(button_tester);
  // expect(button_tester).toHaveBeenCalled();
});
